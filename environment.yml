name: cp-rec
channels:
    - pytorch
    - nvidia
    - defaults
dependencies:
    - python
    - pytorch
    - pytorch-cuda=12.1
    - requests
    - numpy
    - pandas
    - pydantic
    - pip
    - pip:
          - types-requests
